{
  "family": "txtai-rag-frontend",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "arn:aws:iam::076656108307:role/ecsTaskExecutionRole",
  "taskRoleArn": "arn:aws:iam::076656108307:role/ecsTaskRole",
  "containerDefinitions": [
    {
      "name": "txtai-rag-frontend",
      "image": "076656108307.dkr.ecr.us-east-1.amazonaws.com/txtai-rag-frontend:latest",
      "essential": true,
      "portMappings": [
        {
          "containerPort": 80,
          "protocol": "tcp"
        }
      ],
      "environment": [
        {
          "name": "VITE_API_URL",
          "value": "https://YOUR_API_GATEWAY_URL"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/txtai-rag-frontend",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "ecs"
        }
      },
      "healthCheck": {
        "command": [
          "CMD-SHELL",
          "curl -f http://localhost:80 || exit 1"
        ],
        "interval": 30,
        "timeout": 5,
        "retries": 3
      }
    }
  ]
}

